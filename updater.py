from GsheetConnect import sh
from financialCalc import new_index, final_table




#sh.sheet1.update('A2:A'+str(len(new_index)+1), new_index) #sending the Date index for all the data
#sh.sheet1.update('B2:L'+str(len(final_table)+1), final_table) #sending all the data to the spreedsheet


